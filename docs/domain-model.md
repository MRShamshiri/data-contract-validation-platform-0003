# Domain Model

The project tracks work items with ownership, lifecycle status, priority, scoring, and tags. The model is intentionally small but designed to be extended into real ingestion and policy workflows.
